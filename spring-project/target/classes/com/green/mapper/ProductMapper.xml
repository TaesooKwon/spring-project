<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.mapper.ProductMapper">
    <select id="getList" resultType="com.green.vo.ProductVO">
        select * from product
    </select>
    <select id="read" resultType="com.green.vo.ProductVO">
        select * from product where id=#{id}
    </select>
    <insert id="insert">
        insert into
        product(id,product_name,product_price,product_discount,product_totalprice,product_stock,product_img,product_date)
        values
        (#{id},#{product_name},#{product_price},#{product_discount},#{product_totalprice},#{product_stock},#{product_img},now())
    </insert>
    <delete id="delete">
        delete from product where id=#{id}
    </delete>
    <update id="update">
        update product
        set product_name =#{product_name}, product_price=#{product_price}, product_discount=#{product_discount},
        product_totalprice=#{product_totalprice},
        product_stock =#{product_stock}, product_img=#{product_img}
        where id = #{id}
    </update>
</mapper>